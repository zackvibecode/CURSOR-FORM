import { DemoForm } from "@/components/demo/DemoForm";

export const metadata = {
  title: "Demo Form — ZAQONE.FORM",
  description: "Try the ZAQONE.FORM customer-facing enquiry form with WhatsApp submission.",
};

export default function DemoPage() {
  return <DemoForm />;
}
