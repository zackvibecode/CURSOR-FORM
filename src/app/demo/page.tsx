import { DemoForm } from "@/components/demo/DemoForm";

export const metadata = {
  title: "Demo Form — OneForm",
  description: "Try the OneForm customer-facing enquiry form with WhatsApp submission.",
};

export default function DemoPage() {
  return <DemoForm />;
}
